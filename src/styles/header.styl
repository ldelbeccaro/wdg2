@require 'styles'

header
  display flex
  justify-content space-between
  align-items center
  padding 40px
  font-size $fontL
  font-weight 300
  text-transform uppercase

h1
  font-family 'Roboto'
