@require 'styles'

header
  display flex
  justify-content space-between
  align-items center
  padding 40px

  h1
    font-family 'Roboto'
    font-size $fontL
    text-transform uppercase
    font-weight 300
